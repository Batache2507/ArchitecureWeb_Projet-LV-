class Song {
    constructor (id, title, duration, user_id)
    {
        this.id = id;
        this.title = title;
        this.duration = duration;
        this.user_id = user_id;
    }
}

module.exports = Song;